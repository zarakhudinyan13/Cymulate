{
  "devDependencies": {
    "@types/mocha": "^10.0.7",
    "cypress": "^13.13.2",
    "cypress-xpath": "^2.0.1",
    "ts-node": "^10.0.0",
    "typescript": "^4.0.0"
  },
  "scripts": {
    "cypress:open": "ts-node --project tsconfig.json node_modules/cypress/bin/cypress open",
    "cypress:run": "ts-node --project tsconfig.json node_modules/cypress/bin/cypress run"
  }
}
